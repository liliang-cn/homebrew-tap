# liliang-cn/homebrew-tap

Homebrew tap for [TagIt](https://github.com/liliang-cn/tagit) — an open-source, self-hosted Claude Tag.

```sh
brew install --HEAD liliang-cn/tap/tagit
```

Installs the `tagit` CLI and `tagitd` daemon (builds from `main`; requires Go).
